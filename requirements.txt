# Goodware v3.0 — Python dependencies (Pip)
# Necessárias para o cérebro, API REST, federated, prediction, sensors.
# As deps nativas (liboqs, YARA, ClamAV, nftables, tpm2-tools) são instaladas por scripts/install_deps.sh.

# API REST + WebSocket
Flask>=3.0
Flask-Cors>=4.0
Flask-SocketIO>=5.3

# Process introspection
psutil>=5.9
watchdog>=3.0
yara-python>=4.3

# Config / Serialização
PyYAML>=6.0
pydantic>=2.5

# Machine Learning (ThreatPredictor, AnomalyForecast)
scikit-learn>=1.3
joblib>=1.3

# Crypto
cryptography>=41.0

# Network / HTTP client (federated learning)
requests>=2.31
httpx>=0.25

# LLM client (DeepSeek via NVIDIA integrate API)
openai>=1.0

# Logging / UX
rich>=13.0
